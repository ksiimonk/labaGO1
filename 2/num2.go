package main

// Импорт библиотеки
import "fmt"

// Объявление переменных + вывод
func main() {
	var per1 int = 1
	var per2 float64 = 2.2332342432
	var per3 string = "Строка"
	var per4 bool = true
	fmt.Println(per1, per2, per3, per4)
}
